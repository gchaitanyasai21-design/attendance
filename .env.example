# Core app settings
SECRET_KEY=replace-with-a-long-random-secret
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require&channel_binding=require

# Optional local SQLite fallback (used only when DATABASE_URL is empty)
ATTENDANCE_DB_PATH=attendance.db

# Optional email/OTP settings
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_USE_TLS=1
MAIL_FROM=

# Optional defaults
TEACHER_USERNAME=teacher
TEACHER_PASSWORD=teacher123
TEACHER_EMAIL=teacher@college.local
ADMIN_TEACHER_USERNAME=HODMIC@
ADMIN_TEACHER_PASSWORD=hod123@
ADMIN_TEACHER_EMAIL=hodmic@college.local
